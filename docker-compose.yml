services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: docusense
      POSTGRES_PASSWORD: docusense123
      POSTGRES_DB: docusense_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: